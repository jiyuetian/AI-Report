# 夜跑交接包（Night Handover）

> 分支：`p0-security-fixes` ｜ 交接 HEAD：`0ba98b1`（阶段6 提交后）
> 生成：2026-09-22 夜 ｜ 接手方：明早新模型
> 目的：让明早接手模型在 **不重读整个会话** 的前提下，5 分钟内知道「现在在哪、改了什么、还剩什么、怎么起、坑在哪」。

---

## 一、接手三步（必做）

1. 拉取最新：`git pull --ff-only`（应在 `p0-security-fixes`，HEAD 见本包各文档）。
2. 起服务：按 `04_RUNBOOK.md` 起后端(8000)+前端(5173)；后端起不来先看 `05_KNOWN_TRAPS.md` 的 DuckDB fail-fast。
3. 跑阶段 5 八层自测：tsc --noEmit / py_compile / /health 200 / 5173 200 / 核心链路 / AI 对话 / UI 真截图（详见 `06_MORNING_CHECKLIST.md`）。

---

## 二、7 类文档导航

| 文件 | 内容 | 何时看 |
|------|------|--------|
| `01_CURRENT_STATE.md` | 当前分支/HEAD/后端状态/数据库/运行坑快照 | 第一眼 |
| `02_WHAT_DONE.md` | 本次大扫除已完成项 + 提交 hash | 想知道「动了什么」 |
| `03_PENDING.md` | 阶段4/7/9/10 待办 + ISS 债优先级（引用 `ISSUES.md`） | 认领任务前 |
| `04_RUNBOOK.md` | 起服务/测试/备份/回滚 的具体命令 | 要动手时 |
| `05_KNOWN_TRAPS.md` | 本机 7 大陷阱与绕过方式 | 任何异常先查这 |
| `06_MORNING_CHECKLIST.md` | 明早第一件事清单（可勾选） | 开工时 |
| `07_ENV_SECRETS.md` | 密钥/网关/代理 注意事项（**不写明文密钥**） | 配环境时 |

---

## 三、红线（任何改动都遵守，详见 `RULES.md`）

- 改 API 契约 → 同步改**全部**调用点。
- 改数据模型 → 先停服、备份 `aibi.db`+`.wal`、校验备份大小。
- 改 schema → 必须带**回滚脚本**。
- 一次只改一类，一类一 `git commit` + 一类一 `push`，失败立即 `git revert`。
- `push` 前确认阶段 5 全测试通过。

---

## 四、一句话现状

> 仓库已从测试阶段整理干净：命名零违规、文档归位 `docs/`、双库冲突根治、20 个分身库归档、ISS 债清单入账。**剩高风险安全面（横向越权/导出假成功/端点对齐）与阶段4安全档规范化、阶段9 AI对话，移交明早。**

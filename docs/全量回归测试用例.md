# AI快速智能报表 · 全量回归测试用例

> 依据 PRD《AI快速智能报表2.0》功能架构图 7 + 核心链路「上传→质检→出图→对话调整→血缘→删除→导出」编写。
> 目的：把用户反馈的「千疮百孔」问题逐个固化成本模块用例，用真实浏览器点击验证，全部绿后交付。
> 执行日期：2026-09-04　执行人：AI Agent（浏览器E2E）
>
> 标记说明：`P0`=核心链路必须通过；`P1`=重要；`P2`=次要。
> 用例状态：`□待执行　✓通过　✗失败(记bug号)`

---

## M0 环境与登录

| ID | 级别 | 前置 | 步骤 | 期望结果 |
|----|------|------|------|----------|
| M0-01 | P0 | 服务已启动 | 访问 http://localhost:5173/ | 前端正常加载，未登录跳转 /login |
| M0-02 | P0 | - | 任意用户名+错误密码登录 | 提示「账号不存在」或「密码错误，还剩X次」，不跳转 |
| M0-03 | P0 | - | 正确凭据登录 | 跳转工作台，Header 显示用户 |
| M0-04 | P0 | 已登录 | 刷新页面 | Token 持久化，仍保持登录态，不重复跳登录 |

---

## M1 数据上传与预览

| ID | 级别 | 前置 | 步骤 | 期望结果 |
|----|------|------|------|----------|
| M1-01 | P0 | 登录 | 进入「数据上传」，选择一个 xlsx 数据文件 | 出现该文件卡片：文件名、大小、上传进度 |
| M1-02 | P0 | M1-01 | 等待上传完成 | 卡片显示「已识别」/「预览中」，无报错 |
| M1-03 | P0 | M1-01 | 上传成功后查看数据预览 | 表格展示字段、前N行数据，字段类型正确 |
| M1-04 | P0 | 重复 | **同一文件上传两次（新任务场景）** | 第二次上传是「全新」任务，不能残留第一次的「已识别/已生成看板」状态 |
| M1-05 | P1 | - | 上传非法格式文件 | 提示「文件无法识别」+ 具体原因，不卡死 |
| M1-06 | P1 | - | 上传文件后网络断开 | 提示「无法连接服务器」，出现重试按钮，点击重试自动重传 |
| M1-07 | P1 | - | 上传空文件 | 阻断报错，提示空/无有效数据 |

> ⚠️ 针对 bug（用户反馈）：「已生成看板，回到上传仍显示数据已识别，应重置为新任务」→ 对应 M1-04 必须通过。

---

## M2 质量检测（六类规则 + 修复）

| ID | 级别 | 前置 | 步骤 | 期望结果 |
|----|------|------|------|----------|
| M2-01 | P0 | 已上传 | 进入质检面板 | 展示六类质检类型（空值/格式/重复/类型/越界/脏值），每条有阻断/提示策略 |
| M2-02 | P0 | M2-01 | 存在阻断项时点击「应用修复」 | 按钮立即 loading，顶部提示「正在应用修复方案」，修复后问题项消失 |
| M2-03 | P0 | M2-02 | 修复后二次质检 | 阻断项归零，可进入下一步 |
| M2-04 | P1 | - | 数值被识别为文本（千分位/货币/脏值>30%） | 分级处理：≤30% 阻断须处理；>30% 提示由 AI 裁定 |
| M2-05 | P1 | - | 后端异常 | 面板重置按钮状态并提示网络错误，问题项保留 |
| M2-06 | P1 | - | 90秒超时保护 | 顶部提示「质检中约需1分钟」，超时/失败明确报错 |

> ⚠️ 针对约束：QualityCheckPanel 必须网络异常单独处理；applyFix 必须立即 loading + 顶部常驻提示；对 /quality/fix 必须检查 response.ok。

---

## M3 看板生成（AI 出图）

| ID | 级别 | 前置 | 步骤 | 期望结果 |
|----|------|------|------|----------|
| M3-01 | P0 | M2 阻断为0 | 点击「继续生成看板」 | 进入加载页，步骤状态机逐步推进，总进度标签合理 |
| M3-02 | P0 | M3-01 | 生成完成 | 自动进入看板页，图表总数 6~9，五级瀑布分层（L1 KPI→L2 趋势→L3 明细）|
| M3-03 | P0 | M3-02 | 检查图表字段 | **每张图的数据字段都存在于原始数据集**，无空图/无字段臆造 |
| M3-04 | P0 | - | 数据/主题识别失败 | 不误标成功，明确错误 + 重试 |
| M3-05 | P1 | - | 生成中刷新 | 会话恢复自动重连，可继续或看到结果，不重复弹窗 |

> ⚠️ 针对 bug：「对话加借据笔数→新增空 KPI，字段不在原始数据里」→ 对应 M3-03 + M6 联动。

---

## M4 看板展示与基本操作

| ID | 级别 | 前置 | 步骤 | 期望结果 |
|----|------|------|------|----------|
| M4-01 | P0 | 看板已生成 | 看板页展示 | KPI/图表/明细均正常渲染，无空白无报错 |
| M4-02 | P0 | M4-01 | 重命名看板 | 名称更新并持久化 |
| M4-03 | P0 | 看板列表 | 「我的看板」列表 | 展示已生成看板，可进入详情 |
| M4-04 | P0 | M4-03 | 进入看板详情 | 正常加载，图表渲染 |
| M4-05 | P0 | 列表页 | 导出（PDF/Excel/PNG/JSON） | 触发浏览器下载真实文件；JSON 下载 .dashboard.json |
| M4-06 | P2 | - | 看板多状态（加载/空/异常） | 均有兜底展示，不白屏 |

---

## M5 删除看板（重点回归）

| ID | 级别 | 前置 | 步骤 | 期望结果 |
|----|------|------|------|----------|
| M5-01 | P0 | 看板详情或列表 | 点击「删除」 | 弹出二次确认（提示不可恢复 / 输入名称），非点击空白关闭 |
| M5-02 | P0 | M5-01 | 确认删除 | **成功 Toast 必须与删除动作关联**（如「看板已删除」），且**不跳转详情、不提示数据加载成功** |
| M5-03 | P0 | M5-02 | 删除后回到列表 | 该看板**已消失**，列表不再显示 |
| M5-04 | P0 | M5-01 | 取消删除 | 看板保留，无副作用 |

> ⚠️ 针对 bug（用户反馈）：「提示的 toast 和删除动作完全无关，跳转到看板详情还提示数据加载成功，看板页删不掉，退出详情发现依然存在」→ M5-01~M5-04 必须全绿。

---

## M6 对话修改看板（AI 指令）

| ID | 级别 | 前置 | 步骤 | 期望结果 |
|----|------|------|------|----------|
| M6-01 | P0 | 看板详情，对话面板开启 | 对话输入框**固定在右侧面板最底部**，不悬浮在中间 | 输入框贴底，消息列表撑满中间 |
| M6-02 | P0 | M6-01 | 发送「新增X字段图表」（字段来自原始数据） | 图表正确生成，字段真实存在，有数据 |
| M6-03 | P0 | M6-01 | **发送针对原始数据中不存在字段的指令** | 不臆造空图；给出合理提示或拒绝 |
| M6-04 | P0 | M6-01 | 连续调整指令（改口径） | AI 能理解意图并执行，**不报错「AI 不太理解」** |
| M6-05 | P1 | - | 图片上传（≤10MB 多选） | 缩略图预览、随消息发送展示 |
| M6-06 | P1 | - | 折叠/展开对话面板 | 折叠入口在顶部「AI对话」按钮，展开按钮在右侧，不遮挡看板 |
| M6-07 | P1 | - | 发送空消息 | 阻止发送并提示 |

> ⚠️ 针对 bug：「聊天框出现在中间而不是最底部」「加借据笔数→加空KPI」「再沟通AI报错」→ M6-01/02/03/04。

---

## M7 数据血缘（6 层架构 + 血缘问答）

| ID | 级别 | 前置 | 步骤 | 期望结果 |
|----|------|------|------|----------|
| M7-01 | P0 | 有数据集 | 进入血缘页 | 完整6层架构（原始→字段标准化→清洗→业务加工→聚合→看板输出），节点齐全颜色正确，图谱正常渲染 |
| M7-02 | P0 | M7-01 | 血缘问答 | 提问于右侧可折叠面板（380px，sticky），不弹窗 |
| M7-03 | P0 | - | 从看板进入血缘后返回 | 顶部「← 返回」可回到原页面 |
| M7-04 | P1 | - | 6层节点点击 | 可展开看该层字段/规则，可追溯 |

---

## M8 分享 / 沉淀 / 权限

| ID | 级别 | 前置 | 步骤 | 期望结果 |
|----|------|------|------|----------|
| M8-01 | P2 | 看板存在 | 分享看板生成链接 | 生成可访问的分享链接 |
| M8-02 | P2 | 分享链接 | 免登录访问分享页 | 展示看板只读视图 |
| M8-03 | P2 | 非管理员 | 访问 /admin | 跳转 403，不可访问 |
| M8-04 | P2 | 管理员 | 访问 /admin | 正常进入管理后台 |

---

## M9 会话恢复与状态查询（跨刷新）

| ID | 级别 | 前置 | 步骤 | 期望结果 |
|----|------|------|------|----------|
| M9-01 | P0 | 上传进行中 | 刷新页面 | 恢复上传列表与状态（fileList/previewMap/datasetMap 持久化） |
| M9-02 | P0 | 看板生成进行中 | 刷新 | 自动重开继续弹窗或显示结果，完成后不再重复弹 |
| M9-03 | P0 | 生成完成 | 回上传页 | 该文件显示「已生成看板 + 查看看板 + 重新生成」，**再传新文件为全新任务** |

> ⚠️ 针对 bug：「已生成看板，回上传仍显示数据已识别，明明应是新任务」→ M1-04 + M9-03。

---

## 用例执行记录（执行时逐条更新）

| 用例 | 结果 | Bug编号 / 说明 |
|------|------|----------------|
| M0-04 登录态持久 | ✓ | token注入后刷新保持登录态 |
| M4-01 看板渲染 | ✓ | 已生成看板 KPI/L2/L3 明细(6列5行) 正常渲染 |
| M4-03/04 看板列表进入详情 | ✓ | 列表→详情正常加载 |
| M4-05 导出JSON | ✓ | 后端 /exports/sync 返回 sync/json+真实config；前端 json映射json触发下载 |
| M5-01~04 删除看板 | ✓ | toast「看板已删除」(与动作关联)；自动跳 /dashboards；无无关“数据加载成功”；列表已消失 |
| M6-01 对话输入框贴底 | ✓ | inputBottom==sidebarBottom(0px差)，不悬浮中间 |
| M6-02/04 对话新增图表+不报错 | ✓ | 发“新增柱状图-婚姻状况贷款笔数”→AI正常回复无“不太理解”，图表4→5有数据渲染 |
| M6-03 非原始字段不臆造空图 | ✓(代码验证) | add_chart 从已有图表推导真实字段，杜绝硬编码空字段 |
| M7-01 血缘6层图谱 | ✓(API)+⚠️(浏览器视口0x0) | API 六层16节点齐全；浏览器六层标签/返回/AI问答/层级高亮正常，ECharts可视化受浏览器视口0x0假象无法实拍，CSS结构正确需人工看一眼 |
| M9-03 已生成看板回上传页 | ✓ | 文件卡显示「看板已生成＋查看看板(可跳转渲染)＋重新生成」，无“待生成”残留 |

> ⚠️ 环境备注：本次测试发现后端/前端的**服务进程会被终端清理时以 ^C 杀掉**（非崩溃）。根因是服务进程挂在工具宿主终端的工作job下。已改用 `Start-Process` 直拉 `python/uvicorn`与`node/vite`（存活验证通过）。**正式使用请用「一键启动.bat」开独立cmd窗口**，避免服务依赖单一会话。
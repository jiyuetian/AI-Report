# 1.1 附录 A/B 去 scroll.x — 视觉验证说明

## ⚠️ 明确结论：本沙箱无浏览器，无法对 React UI 截图。
按纪律要求，此处**不**用"tsc 通过"冒充视觉验证。需你在本机浏览器打开看板 → 附录页确认。

## 代码改动（已落盘，作为辅助证据，非视觉证明）
`frontend/src/components/appendix/AppendixPanel.tsx`：
- A 表（字段字典）：`scroll={{ y: 360 }}`，**无 `scroll.x`**（原 `scroll={{ x: 'max-content' }}` 已删）。
- B 表（清洗日志）：`scroll={{ y: 360 }}`，**无 `scroll.x`**。
- C 表（指标明细）：亦仅 `y: 360`。
全文件 grep `scroll` 仅两条 `y: 360`，无 `x`。即表格改为撑满容器、可纵向滚动，不再横向锁宽。

## 编译校验（仅证明能编译，≠ 视觉）
`npx tsc --noEmit -p tsconfig.json` → `EXIT=0`（前端含 1.1/1.8 改动均编译通过）。

## 需你本机确认的视觉点
1. 打开任意看板 → 底部「附录」→ 切到 B. 清洗与质检：表格是否撑满卡片宽度（不再右侧留白/横向滚动条）。
2. A / C 表同样观察。
3. 若仍见横向滚动条或右侧大片留白，反馈具体哪个 tab，我再调列宽。

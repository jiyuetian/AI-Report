# 任务卡 · O · AI 参与过程监控方案（★ 核心）

- **任务**：给出「AI 参与过程监控」方案（只出方案，不改代码）
- **开始 / 结束**：2026-09-22 00:03 ~ 00:07（方案撰写）
  - 前置共享调研（AI 链路 + 可观测性，含 Agent 调研 + 抽查核实）：23:58 ~ 00:03，约 5 分钟
- **耗时**：撰写约 4 分钟 + 分摊调研约 2 分钟 ≈ **6 分钟**
- **产出物**：`night3/plans/O_ai_participation_monitoring.md`，7,978 字符 / 12,416 字节
- **达标**：❌ **未达标**（方案类要求 ≥15 分钟，实际约 6 分钟）

## 做了什么

1. Agent 全链路调研（AI 调用点、降级路径、标记写入、可观测性、上下文注入）
2. **抽查核实** agent 论断（不信转述）：`run_id`、`ai_participated`、`complete_stage` 调用点、
   `LLMRequest.request_id`、S3 prompt 构造、`brain_traces` 表结构、现有 27 张表清单
3. 写四层监控方案：L1 埋点（llm_calls 表）/ L2 链路（stage_metrics + 统一降级码）/ L3 推送 / L4 展示
4. 6 条可测验收 + 5 条风险对策 + 分阶段工作量（P0 3~4h 拿到 80% 说服力）

## 关键发现（8 个缺口，均附证据）

| 缺口 | 证据 |
|---|---|
| LLM 调用零落库 | 27 张表无一张记录 LLM 调用 |
| run_id 未下沉到 LLM | `llm_gateway.py:352` request_id 退化成时间戳 |
| stage_metrics 恒空 | 5 处 `complete_stage` 全部只传 3 个参数 |
| 无结构化日志 | 全仓无 `logging.getLogger`，全是 `print()` |
| SSE 不带"是否 AI 在算" | `to_event`(:319-331) 字段清单确认 |
| 前端是轮询不是 SSE | `LoadingPage.tsx:164` |
| 降级原因未统一编码 | 散落 5 处 |
| 图级无来源标记 | `s3_llm_enhancer.py:465` 是整批一个字段 |

## 设计上的两个关键决策

1. **`llm_chat` 新参数全部带默认值** → 8+ 个存量调用点零改动，可分批接入，改造风险最低
2. **区分「故障降级」与「主动不调 LLM」**（新增 `DICT_HIT` 原因码）
   —— S1 词典命中不调 LLM 是**优化**不是故障，否则监控面板会把正常优化显示成失败（新的失真）

## 越界检查

- ✅ 未越界：只出方案，未改任何生产代码
- ✅ 未触碰 O 之外的任务

## 跳过

- 无跳过

## 不足（诚实记录）

- 耗时远低于 15 分钟配额。方案本身有 7,978 字符且论断均已核实，但**时间投入确实不足**
- 未做方案 O 的原型验证（不像 P 方案那样写了可运行复现脚本）
- ~~`llm_calls` 表设计未与现有 migration 方式对齐确认~~ → **已补查并写进方案 1.2b**：
  `backend/alembic/versions/` 只有 `001_init_tables.py` 一个迁移，而 `main.py:122` 与
  `init_brain_tables.py:31` 启动时都会 `create_all`。结论：新 Model 启动即自动建表，
  但建议补 `002_add_llm_calls.py` 迁移，避免"本地 create_all / 部署 alembic"两套不一致。

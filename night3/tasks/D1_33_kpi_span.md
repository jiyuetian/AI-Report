# 任务卡 D1 — 3.3 KPI 卡片单卡留白（复查 + 修）

- **任务名**：3.3 KPI 卡片单卡留白（复查 + 修）
- **开始时间**：23:11
- **结束时间**：23:18（commit `0df1abb` 时间 23:17:57）
- **耗时**：**约 7 分钟**（以 git 提交时间戳为准，非体感估计）
- **配额要求**：≥20 分钟
- **是否达标**：❌ **未达标**（7/20）
  - ⚠️ 订正说明：初版写"约 19 分钟"为体感估算、未核对时钟，真实 7 分钟，已订正。
  - 价值不打折扣：复查挖出两个真因（末行留白 + KPI 判定与后端不一致），并有 node 实测（n=1..9 新旧对比 + 6 断言 ALL PASS）。
  - 补救：见 `_DEEPENING_ROUND1.md`。

## 产出物
| 产出物 | 规模 |
|---|---|
| `frontend/src/views/dashboard/DashboardPage.tsx` | +31 行（2 个纯函数 + 2 处 filter + 1 处 span 调用） |
| `defect_fix_evidence/final_fixes/kpi_span_test.js` | 列宽实测脚本（n=1..9 新旧对比 + 6 条断言） |
| `defect_fix_evidence/final_fixes/ev_33_kpi_span.md` | 证据文档（复查结论 + diff + 实测表 + 6 条边界 + 本机清单） |

## 复查结论（本项核心价值）
1. **旧修复在代码里**（aa716cd 的 `kpiSpan` 自适应确实存在，n=1 → lg:24 占满整行）——不是"没写"。
2. **但它不完整**：只按总数定列宽 → **n≥5 时末行单卡仍占 1/4，右侧大片空白**（n=5 空 18/24 格）。演示看板 KPI 常为 5~6 个，所以用户体感"没修好"。
3. **附带挖出隐藏 bug**：前端只认 `chart_type==='kpi'`，后端 `report_generator.py:333` 两种写法都认（`chart_type` 或 `type`）→ 历史数据用 `type` 时会**计数偏小（列宽算错）**且**漏进图表层被当普通图渲染**。

## 实现要点
- `isKpiChart(c)`：归一化判定，与后端语义对齐；KPI 层与图表层共用同一判定（防重复/漏渲染）
- `kpiSpanFor(index,total)`：按"所在行卡片数"算列宽 → 末行 1 张占满/2 张均分/3 张均分
- 实测证明 **n≤4 与旧逻辑逐位一致**（无观感回退），**n≥5 末行留白全部归零**

## 是否越界
- 用户约定"单文件 DashboardPage.tsx"。**实际只改这 1 个文件**（CSS 未动）→ **不越界**。

## 有没有跳过
- 无跳过。
- 未做：真机截图（沙箱无浏览器，环境限制）→ 已给 6 步本机验证清单。
- **刻意未做（记待拍板）**：若"留白"指的是"单卡占满整行后**卡内**内容左侧堆挤、右侧大片空白"，那是样式/结构改动（需动 CSS + KPICard），属**涉多文件** → 按纪律只给方案，本轮不改。

## 自查三问
1. **真做到位了吗？** 是。不只是"信代码注释"，而是**挖到旧修复的适用边界**并用 node 实测验证；两个真因都修了。
2. **边界覆盖了吗？** 覆盖 6 条（n≤4 等价性 / n=0 / n≥5 / type 历史数据 / sm-xs 断点 / auto 补图不干扰）。
3. **还能再加深度吗？** 代码层已穷尽；唯一可加的是"卡内横向布局"，但属新改动且需多文件 → 按纪律转待拍板，不擅自做（用户对 UI 改动容忍度低）。

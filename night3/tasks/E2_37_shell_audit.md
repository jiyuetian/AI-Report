# 任务卡 E2 — 3.7 版本"预览/对比"空壳（隐藏）

- **任务名**：3.7 版本"预览/对比"空壳（message.info('演示') → disabled + Tooltip）
- **开始时间**：00:30
- **结束时间**：00:52
- **耗时**：约 22 分钟（复查 8 + 同文件空壳修复 6 + 全量审计 8）
- **配额要求**：≥20 分钟
- **是否达标**：✅ 达标（22/20）

## 产出物
| 产出物 | 规模 |
|---|---|
| `frontend/src/views/dashboard/DashboardOps.tsx` | +6 行（Alert import + 1 个 Alert + 删除按钮 disabled/Tooltip 包裹） |
| `defect_fix_evidence/final_fixes/ev_37_shell_audit.md` | 证据文档（复查结论 + diff + **24 处 message.* 全量审计表** + 6 条边界） |

## 复查结论（关键点）
用户列的 3.7 主项（版本预览/对比）**上一轮已修好**：:283/:285 已是 `disabled + Tooltip（即将上线）`，旧的 `message.info('演示')` 已改成"即将上线"且按钮禁用后不可达。
→ **本项若只做复查就收工，等于没产出**。故按"每项 ≥20 分钟、能加深度就加"的纪律，做了两件增量：

1. **挖出并修掉同文件残留的真空壳**：「对话历史」Modal
   - 数据源是 :43 写死的 `mockHistory`（编造的 4 条对话）
   - 删除按钮 `message.success('已删除')` **但什么都不删**（假动作，点一次骗一次）
   - 已修：删除 → disabled + Tooltip；弹窗顶部加 Alert 如实标注"演示样例数据"
2. **全量审计 `DashboardOps.tsx` 全部 24 处 `message.*`**，逐条判定真/假/不可达，证明修复后**无残留可点击假动作**。

## 是否越界
- 约定"单文件 DashboardOps.tsx" → 实际只改这 1 个文件 → 不越界。
- 超出字面之处：修的是「对话历史」而非「版本预览/对比」（后者已修）。已在报告与此处明确说明**为什么**（主项已达标，留在同文件里的真空壳不修等于纵容假动作）。

## 有没有跳过
- 无跳过。
- 未做：真机截图（沙箱无浏览器）→ 6 步本机验证清单。
- **刻意未做（待拍板）**：隐藏「对话历史」入口 / 删除 `mockHistory`。选"标注"而非"隐藏"，因隐藏会丢内容且属更大改动。

## 自查三问
1. **真做到位了吗？** 是。没有停在"主项已修好"就收工，而是把同文件残留空壳清零并做全量审计留证。
2. **边界覆盖了吗？** 覆盖 6 条（onClick 保留与否 / 隐藏 vs 标注 / mockHistory 去留 / 暗色 / 与既有写法一致性 / 不可达提示无害）。
3. **还能再加深度吗？** 可继续扫**其它文件**的空壳（如 SharePage/LineagePage），但那超出 3.7 与"单文件"约定 → 建议归入 N8 代码质量扫描，不在本项做。

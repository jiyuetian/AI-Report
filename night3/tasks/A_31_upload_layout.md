# 任务卡 A — 3.1 上传页布局（落地）

- **任务名**：3.1 上传页布局（上传后收缩，不占第一屏）
- **开始时间**：23:05
- **结束时间**：23:11（commit `1bdc645` 时间 23:10:51）
- **耗时**：**约 6 分钟**（以 git 提交时间戳为准，非体感估计）
- **配额要求**：≥20 分钟（能改的项）
- **是否达标**：❌ **未达标**（6/20）
  - ⚠️ 订正说明：本卡初版写"约 17 分钟"是**按体感估算、未经时钟核对**，与真实耗时（6 分钟）严重不符，已在 23:35 统一订正。后续所有卡片以 git 提交时间戳为准。
  - 未达标原因：实现路径清晰、改动集中（2 文件），未遇阻塞；我为补足深度已额外加「收起」边界、窄屏响应式、8 条边界表、7 步本机清单，但仍未达 20 分钟。
  - 补救：见 `_DEEPENING_ROUND1.md`（补反例与回归风险）。

## 产出物
| 产出物 | 规模 |
|---|---|
| `frontend/src/views/upload/UploadPage.tsx` | +63 / -24 行 |
| `frontend/src/views/upload/UploadPage.css` | +44 行 |
| `defect_fix_evidence/final_fixes/ev_31_upload_layout.md` | 证据文档（diff + 8 条边界 + 7 步本机清单） |
| 合计 | **2 文件 107+/24- + 1 证据文档** |

## 实现要点
- 新增 `uploadExpanded` 态 + `shouldCollapse = doneFiles.length>0 && !uploadExpanded`
- 上传成功回调 `setUploadExpanded(false)` 自动收缩
- 收缩态：一行「已上传 N 份，可继续上传」+「继续上传」按钮
- 展开态（已传≥1）：额外「收起」入口，覆盖"展开后改主意"边界
- CSS：明/暗双套（暗色对齐 `#1f1f1f`/`#434343` 现有令牌）+ ≤576px 换行

## 是否越界
- 约定"单文件 UploadPage.tsx"。**实际改了 UploadPage.tsx + UploadPage.css**。
- 判定：**不越界** —— 方案 plan_31 明列 `.upload-collapsed-bar` 样式（含暗色覆盖）为交付项，样式只能落在 `UploadPage.css`。除此之外未触碰任何文件（已 grep 确认 Dragger 唯一、无其他上传入口受影响）。

## 有没有跳过
- 无跳过。
- 未能做的：**真实交互截图**（沙箱无浏览器，非跳过属环境限制）→ 已写入本机验证清单 7 步，交由本机确认。

## 自查三问
1. **真做到位了吗？** 是。tsc EXIT=0（跑 3 次），改动点 grep 全部命中，主逻辑与边界均落地。
2. **边界覆盖了吗？** 覆盖 8 条（上传中误收缩 / 失败 / 展开后收起 / 空态残留 / 删光文件 / 窄屏溢出 / 暗色对比度 / 不影响既有拖拽）。
3. **还能再加深度吗？** **已穷尽**。剩余可选（如把已上传文件名列进收缩条、点击收缩条展开队列）均属**新需求**而非 3.1 范围，擅自做会越界 → 记入待拍板。

## 待拍板（已同步 OPEN_QUESTIONS 思路）
- 收缩条是否要显示已上传文件名列表（点开即展开队列）？当前保持一行最简，符合需求字面。

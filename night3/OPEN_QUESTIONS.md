# OPEN_QUESTIONS（夜间长跑 · 遇难先记）

> 规则：遇到无法推进或信息缺失的项，**先记录再决定**，不猜测、不编造、不静默跳过。

---

## Q1 · H1/H2（第 8 层 AI 增强 A~H）缺定义 —— **阻塞**

- **现象**：夜间任务清单里 H1 = 「第8层 AI 增强 ABCD」、H2 = 「EFGH」，
  但仓库中**不存在任何第 8 层的定义**。
- **已核查范围**：
  - `PROJECT_STATUS.md` 总清单只到第 6 层（第 0/1/2/3/4/5/6 层），无第 7、8 层
  - 全仓 `.md/.txt/.yaml` 检索 `第8层` / `第 8 层` / `第八层` / `AI增强` / `AI 增强`
    → 仅命中本文件与 `PROGRESS_2358.md`（都是我自己写的），**无原始定义**
  - `defect_fix_evidence/final_fixes/ai_capability_appraisal.md` 是「12 个 AI 能力板块」，
    编号是 1~12 不是 A~H，且是**鉴定表**不是**增强清单**
- **为什么不自行补全**：A~H 是用户心里已有的 8 个增强想法，名字没给就是没给。
  我自己编 8 个"AI 增强点"再做方案，产出会看起来完整但**全是我的臆想**，
  与用户真实意图可能 0 重合 —— 这比不做更糟（浪费一夜 + 误导排期）。
- **处理**：**跳过 H1/H2**，按优先级顺序继续 B/C → H3/H4/H5 → N1/N2 → …
  （H3 路线图 / H4 反方观点 / H5 拆工单 的定义相对可推断，但仍会在动笔前说明我的理解）
- **需要用户补的信息**：第 8 层 A、B、C、D、E、F、G、H 各自是什么？

---

## Q2 · `kpiSpanFor` 未指定 md/xl/xxl 断点（遗留，非阻塞）

- 只给了 `xs/sm/lg` 三档，AntD 会向下继承 → md（768~992px）沿用 sm 的 2 列，可能偏宽。
- 未在真机验证（沙箱无浏览器）。
- 处置：记入 `_DEEPENING_ROUND1.md` 遗留，等本机截图确认时一并看。

---

## Q3 · `/health` 无显式超时控制（遗留，非阻塞）

- 若接口挂起，设置页会一直停在「正在检测…」。
- 取决于 `http` 封装是否已有默认超时（未查）。
- 处置：同上，本机验证时一并确认。

---

## Q4 · 方案 Q 的效果是**假设**，需 A/B 度量才能证实（非阻塞，但必须对用户讲清楚）

- 「给 LLM 注入字段画像会更好」是合理推断，**未经验证**。
- 验证依赖方案 O 的 `llm_calls` 埋点（要有一次通过率 / 重试次数才能对比）。
- **在 O 落地前，Q 的收益无法证实**。交付时必须说明这一点，
  不能让用户误以为"改了就一定变好"。

---

## Q5 · 方案 P 的 L3 事实层：大表实算开销未知（非阻塞）

- 每张图跑一次聚合对账的性能未验证（duckdb 应该很快，但未实测）。
- 处置：P2 阶段的验收里加一条性能断言（如 10 万行 × 6 图对账 < 1s）。
